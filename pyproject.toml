[tool.poetry]
name = "real-estate-os"
version = "0.1.0"
description = "A real estate data processing and outreach platform."
authors = ["Cody Bias <codybias9@gmail.com>"]
readme = "README.md"
package-mode = false
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
# Core agent dependencies - add more as needed
scrapy = "^2.11.2"
playwright = "^1.45.0"
boto3 = "^1.34.138"
pydantic = "^2.8.2"
requests = "^2.32.3"

[tool.poetry.group.dev.dependencies]
pytest = "^8.2.2"
black = "^24.4.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
